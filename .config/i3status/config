order += "window_title_async"
# order += "spotify" # why doesnt this work :(
order += "backlight"
order += "volume_status"
order += "sysdata"
order += "battery_level"
order += "wifi"
# order += "online_status"
order += "clock"

sysdata{
	cache_timeout = 5
	format = "[\?color=cpu CPU: {cpu_usage}%] [\?color=mem Mem: {mem_used}/{mem_total} {mem_unit}]"
}

battery_level{
	battery_id = "all"
	format = "{percent}% {time_remaining}"
}
